# Settings are split into environment-specific modules.
#
# Switch between them by setting the DJANGO_SETTINGS_MODULE env var:
#   - config.settings.development  (default, uses SQLite)
#   - config.settings.production   (uses PostgreSQL)
